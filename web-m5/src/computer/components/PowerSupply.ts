import Component from "./Component";

export default class PowerSupply extends Component {
    // Props


    constructor(
        desc: Component,
        components: {},
        props: {  }
    ) {
        super(desc);
    }
}